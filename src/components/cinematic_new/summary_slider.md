# Summary: cinematic_new slider

## Задача

Нужно привести `src/components/cinematic_new` к Figma-референсу:
https://www.figma.com/design/wvU80E5h11zr2RbfUkk8yc/design?node-id=506-802&m=dev

Целевой результат: бесконечный WebGL-видеослайдер в виде изогнутой ленты/киноленты. На экране должны быть видны только 3 смысловых кадра: широкий центральный и два боковых, обрезанных краями viewport примерно наполовину или немного больше. Между центральным и боковыми нужен зазор около 20-70px. Слайды не должны наслаиваться друг на друга: соседние кадры не перекрывают центральный и не заходят поверх видимой области друг друга. Переходы должны быть плавными, с ощущением прокрутки киноленты, без моргания боковых кадров.

## Текущий статус

Текущая архитектура выбрана правильно:

- `CinematicVideoSlider.client.tsx` держит React client boundary, DOM overlay, кнопки, keyboard/pointer handling.
- `SliderScene.ts` держит Three.js scene, camera, renderer, video lifecycle, resize/visibility observers и GSAP timelines.
- `VideoPlane.ts` инкапсулирует mesh + shader uniforms одного видеокадра.
- `shaders/videoPlane.ts` отвечает за cover-кроп, изгиб, opacity, затемнение, радиус и motion distortion.
- DOM-оверлей отделен от WebGL, что правильно для Next.js App Router и снижает риск hydration mismatch.

Архитектуру не нужно заменять на DOM carousel, Embla или React Three Fiber. Правильнее сохранить vanilla Three.js + ShaderMaterial + GSAP.

## Основные проблемы сейчас

- Центральный кадр слишком узкий относительно Figma: сейчас примерно `0.58 * viewport width`, целевой диапазон ближе к `0.70-0.72`.
- Расстояние между кадрами считается как `frameStep = centerWidth * 0.68`, поэтому кадры читаются как широкая непрерывная полоса, а не как 3 отдельные части киноленты.
- Текущее перекрытие/слипание кадров недопустимо: целевой эффект должен быть лентой с управляемыми зазорами, а не слоями, наложенными друг на друга.
- Нет строгой модели: center / side / buffer / sleeping.
- Боковые кадры не имеют предсказуемой видимой доли.
- Дальние кадры скрываются слишком поздно или слишком резко.
- Возможное моргание связано с тем, что будущий боковой кадр появляется без стабильной offscreen-buffer позиции.
- `resetInactiveVideo()` может сбрасывать видео сразу после выхода за viewport, что рискованно при быстром обратном движении.
- Desktop-адаптивность пока не оформлена как контракт: композиция должна быть пропорциональна экрану на 1280, 1440, 1920 и wide desktop.

## Оценка предложенных решений

### 1. Оставить текущую архитектуру Three.js + ShaderMaterial + GSAP

Вероятность успеха: высокая.

Это правильная база для эффекта киноленты. Нужны деформация плоскостей, плавный morph в fullscreen, управление видео-текстурами и синхронная анимация uniforms. DOM-карусель или Embla хуже подходят, потому что изгиб и fullscreen morph пришлось бы имитировать CSS-трансформами и масками.

Риск: средний только по производительности, если держать слишком много активных видео. Его снижает правило: играть должно только активное видео, остальные держать как paused preview/poster.

### 2. Переписать layout-модель через роли кадров

Вероятность успеха: высокая.

Роли `center`, `side`, `buffer`, `sleeping` прямо решают главную проблему: visual contract становится явным. Вместо случайного результата от `frameStep` появится управляемая композиция:

- `0`: центральный кадр;
- `-1` и `+1`: боковые видимые кадры;
- `-2` и `+2`: буферные кадры за viewport;
- остальные: sleeping.

Риск: низкий. Нужно аккуратно сохранить `centeredOffset()` и `slidePosition`, чтобы не сломать бесконечность.

### 3. Добавить offscreen-buffer для боковых слайдов

Вероятность успеха: высокая для устранения моргания.

Это наиболее важная доработка для бесшовного движения. Буферные кадры `±2` уже должны существовать, иметь готовые uniforms и texture, но находиться за viewport. Когда пользователь листает, следующий кадр входит не из пустоты, а из подготовленной позиции.

Риск: средний. Если все буферные видео будут реально играть, будет лишняя нагрузка. Правильная реализация: буферные кадры не проигрывать, но держать валидный first frame/poster/paused VideoTexture.

### 4. Перенести reset видео из viewport-exit в sleeping-role

Вероятность успеха: высокая.

Сейчас сброс при полном выходе за viewport может быть слишком ранним. Для плавной карусели кадр, который только что ушел за край, еще может понадобиться при обратном движении. Сбрасывать его нужно только после ухода за buffer-зону, например `absOffset > 2.5`.

Риск: низкий. Нужно заменить условие в `applySliderLayout()` и хранить previous role/status.

### 5. Ввести плавную fade-зону за viewport

Вероятность успеха: высокая.

Жесткое `opacity: isFullyOutside ? 0 : 1` создает риск визуального щелчка. Лучше считать opacity от расстояния за viewport через `smoothstep`. Тогда кадр мягко появляется и исчезает в зоне за краем, а не внутри видимой области.

Риск: низкий. Важно не нарушить требование "видно только 3 кадра": buffer должен оставаться за viewport или быть невидимым до входа.

### 6. Desktop-пропорциональность от viewport

Вероятность успеха: высокая.

Figma-вид можно стабилизировать через расчеты от `viewport.x` и `viewport.y`:

- центр: примерно `70-72%` ширины viewport;
- высота: через aspect и лимит от высоты viewport;
- gap: `clamp(20, viewport.x * ratio, 70)`;
- side visible: явная доля или явная видимая ширина.

Риск: средний. На wide desktop слишком широкая сцена может стать плоской, поэтому нужны проверки на 1280, 1440, 1920 и wide viewport. Возможно потребуется max-ratio или ограничение высоты.

### 7. Настроить профиль движения через GSAP

Вероятность успеха: средне-высокая.

Текущий подход с `slidePosition` уже близок к нужному. Улучшения:

- сохранить непрерывное движение `slidePosition`;
- не переставлять mesh вручную;
- скорректировать duration/ease;
- держать `uVelocity` активным достаточно долго, чтобы движение читалось как прокрутка ленты;
- уведомлять React об active slide ближе к прохождению центра.

Риск: средний. Хорошее ощущение киноленты зависит от ручной подстройки easing, bend, velocity и layout. Это нужно проверять глазами и через Playwright screenshots/video-like interaction.

### 8. Shader-изгиб оставить, но подстроить под новую раскладку

Вероятность успеха: средняя-высокая.

Текущий shader уже поддерживает глобальный X через `uStripOffset`, `uBend`, `uEdgeCurve`, `uVelocity`. Это правильная основа. Вероятно, хватит настройки uniforms. Если лента будет выглядеть как отдельные карточки, потребуется легкая правка vertex shader для более общей дуги.

Риск: средний. Слишком большой bend может визуально ломать видео, слишком маленький не даст эффекта киноленты.

## Итоговая оценка успеха

Предложенный план с высокой вероятностью приведет к целевому результату, потому что он исправляет именно первопричины:

- композиция станет заданной, а не случайной;
- desktop будет пропорционален viewport;
- боковые кадры будут обрезаться предсказуемо;
- слайды будут разделены controlled gaps и не будут наслаиваться друг на друга;
- offscreen-buffer уберет моргание;
- reset видео перестанет конфликтовать с быстрым обратным движением;
- текущая WebGL/GSAP архитектура сохранит возможность изогнутой ленты и fullscreen morph.

Основной риск не архитектурный, а визуально-настроечный: потребуется итеративно подобрать ratios, `bend`, `edgeCurve`, `rotationY`, darkness, opacity fade и ease. Это нормальный риск для такого motion/UI эффекта.

## План выполнения

### 1. Подготовить layout contract - выполнено

Результат этапа: в `SliderScene.ts` появляется явная модель геометрии, а не набор разрозненных чисел.

- [x] Ввести `FilmStripLayoutConfig`.
- [x] Ввести роли `center | side | buffer | sleeping`.
- [x] Описать desktop и mobile config отдельно.
- [x] Оставить `slidePosition` и `centeredOffset()` как основу бесконечной карусели.

Итог: этап подготовил контракт для следующих шагов без переписывания текущей геометрии. В `SliderScene.ts` добавлены `FilmStripLayoutConfig`, `FilmStripSlideRole`, desktop/mobile config, `getFilmStripLayoutConfig()` и `getSlideRole()`.

### 2. Переписать расчет размеров - выполнено

Результат этапа: слайдер пропорционален экрану на desktop.

- [x] Центральный кадр считать от viewport: примерно `70-72%` ширины.
- [x] Высоту считать через aspect и ограничение от viewport height.
- [x] Gap считать через `clamp(20, viewport.x * ratio, 70)`.
- [x] Side visible считать явно, а не получать случайно из `frameStep`.

Итог: в `SliderScene.ts` добавлен `getFilmStripFrameMetrics()`. `getLayoutForOffset()` теперь берет `centerWidth`, `centerHeight`, `sideWidth` и `sideHeight` из пропорциональных metrics. Позиции слайдов пока остаются старой формулой и будут исправляться на этапе 3.

### 3. Переписать позиции слайдов - выполнено

Результат этапа: видны 3 кадра, боковые обрезаны краями viewport, overlap отсутствует.

- [x] `offset 0` поставить в центр.
- [x] `offset -1` и `+1` поставить в боковые позиции.
- [x] Расчет `sideX` делать от `centerWidth / 2 + gap + sideWidth / 2`.
- [x] Проверить, что боковые слайды не заходят поверх центрального.
- [x] Проверить, что соседние слайды не перекрывают друг друга.

Итог: `getLayoutForOffset()` больше не использует старый `frameStep = centerWidth * 0.68` для X-позиций. Позиция считается через `sideX`, где между центральным и боковым кадром заложен явный gap. Это убирает архитектурную причину overlap между тремя основными кадрами. Offscreen-buffer и fade появление будут дорабатываться на этапах 4-5.

### 4. Добавить offscreen-buffer - выполнено

Результат этапа: будущие боковые кадры готовы до входа в viewport.

- [x] Для `offset -2` и `+2` задать buffer-позиции за viewport.
- [x] Не показывать buffer внутри видимой области.
- [x] Не добавлять DOM-клоны.
- [x] Использовать существующие `VideoPlane[]` и loop math.

Итог: `getLayoutForOffset()` теперь использует роли через `getSlideRole(offset)`. Видимыми остаются только `center` и `side`, а `buffer`/`sleeping` скрыты через opacity. Для offsets за боковой позицией добавлена отдельная buffer-дистанция, чтобы будущие кадры существовали за viewport без DOM-клонов. На этом же этапе уточнен расчет `sideVisibleWidth`: боковая видимость теперь фактически равна свободному месту между краем viewport и центральным кадром с учетом gap, что сохраняет отсутствие overlap.

### 5. Убрать моргание - выполнено

Результат этапа: при движении влево/вправо боковые кадры входят бесшовно.

- [x] Заменить жесткое `opacity: isFullyOutside ? 0 : 1` на fade-зону за viewport.
- [x] Не сбрасывать видео сразу после выхода за viewport.
- [x] Перенести `resetInactiveVideo()` на роль `sleeping`.
- [x] Проверить, что buffer-кадры имеют готовый first frame / paused texture.

Итог: `getLayoutForOffset()` теперь умножает role opacity на viewport fade opacity. Buffer-кадры могут мягко входить из-за viewport вместо резкого появления. `applySliderLayout()` больше не сбрасывает inactive video по факту выхода за viewport; reset выполняется только при переходе кадра в `sleeping`. Для этого `SlideVideo` хранит `lastRole`.

### 6. Настроить motion - выполнено

Результат этапа: движение ощущается как прокрутка киноленты.

- [x] Оставить движение через GSAP-анимацию `slidePosition`.
- [x] Подобрать `duration` и `ease` для `slideTo()`.
- [x] Настроить `slideVelocity`, чтобы `uVelocity` не исчезал слишком рано.
- [x] Убедиться, что active index меняется около прохождения центра.

Итог: `slideTo()` теперь анимирует `progress` вместе с `position`, а `slideVelocity` считается через sinusoidal pulse. Это возвращает более выраженную скорость в середине перехода и не обнуляет motion distortion слишком рано. Duration уменьшен до `1.28`, ease изменен на `power2.inOut`, а влияние velocity в layout усилено.

### 7. Настроить визуальный изгиб - выполнено

Результат этапа: кадры выглядят как части одной изогнутой киноленты.

Перед этапом: по запросу увеличена высота слайдов без изменения ширинных ratios. В `SliderScene.ts` изменены `centerAspect` и `maxHeightRatio` для desktop/mobile config. Дополнительно добавлено tall desktop правило: при viewport height от `960px` слайдер стремится к высоте `550px`, но не выше `52%` высоты viewport и не выше ограничения от aspect.

Важная корректировка перед этапом 7: все видимые слайды должны быть выровнены по высоте. Боковые кадры больше не уменьшаются по высоте относительно центрального, потому что цельность киноленты важнее перспективного scale-down. Из этапа 3 нужно сохранить ощущение непрерывной прокрутки: кадры должны двигаться как одна последовательная лента по общей координате, но без overlap и без потери боковой видимости.

Подготовительная shader-правка перед этапом 7: добавлен `uObjectPosition` для CSS-like `object-fit: cover` / `object-position` поведения. По умолчанию используется `vec2(0.5, 0.5)`, поэтому геометрия, размеры, gap и движение не меняются. Это дает возможность позже тонко кадрировать центральный/боковые кадры без изменения layout.

Дополнительная правка после проверки: активный центральный слайд использует contain-style кадрирование внутри shader, чтобы видео помещалось полностью в центральный кадр. Боковые кадры остаются cover-style. Для движения добавлен динамический `renderOrder`: во время sliding уходящий центральный кадр не должен рисоваться поверх бокового/входящего кадра в сторону движения.

- [x] Подобрать `bend`.
- [x] Подобрать `edgeCurve`.
- [x] Подобрать `rotationY`.
- [x] Подобрать `darkness`.
- [x] Проверить `renderOrder`, чтобы он не создавал overlap.
- [x] Править shader только если uniform-настроек недостаточно.

Итог: `bend`, `edgeCurve`, `rotationY` и `darkness` теперь интерполируются через `lerpByStripRole()` от `center` к `side` и `buffer`, используя film-strip config. `scaleY` для видимых кадров зафиксирован в `1`, чтобы не ломать общую высоту киноленты. `renderOrder` переведен на роли: center выше side, side выше buffer, sleeping ниже всех.

### 8. Проверить fullscreen open/close

Результат этапа: новая геометрия не ломает открытие активного слайда.

- Проверить `open()` из центрального кадра.
- Проверить `close()` и возврат в точную slider-позицию.
- Убедиться, что fullscreen использует тот же `VideoTexture`, без второго DOM-video.

### 9. Подогнать UI под Figma

Результат этапа: WebGL-лента и DOM overlay визуально соответствуют референсу.

- Заменить плоский фон на темный фон с мягким свечением, если нужна строгая близость к Figma.
- Проверить позицию заголовка.
- Проверить позицию подписи активного проекта.
- Проверить, что DOM-текст не перекрывает нецелевые зоны.

### 10. Проверить адаптив

Результат этапа: desktop всегда пропорционален экрану, mobile не ломается.

- Проверить desktop `1280x720`.
- Проверить desktop `1440x900`.
- Проверить desktop `1920x1080`.
- Проверить wide desktop.
- Отдельно проверить mobile layout и при необходимости упростить геометрию.

### 11. Провести runtime-проверку

Результат этапа: нет явных runtime/hydration/WebGL проблем.

- Проверить через Playwright screenshot.
- Проверить canvas nonblank.
- Проверить движение next/previous.
- Проверить отсутствие browser console errors.
- Проверить Next MCP `get_errors`.

### 12. Финальная валидация

Результат этапа: изменение готово к ревью.

- Запустить `npm run lint`.
- Запустить `npm run build`.
- Зафиксировать, какие файлы изменены.
- Сравнить результат с критериями готовности.

## Критерии готовности

- На desktop видны только 3 кадра.
- Центральный кадр самый широкий.
- Боковые кадры обрезаны краями viewport примерно наполовину или больше.
- Gap между центром и боковыми стабилен в диапазоне 20-70px.
- Слайды не наслаиваются друг на друга.
- Движение бесконечное и плавное.
- При движении влево/вправо боковые слайды не моргают.
- Видно ощущение единой изогнутой киноленты.
- Видео не растягивается и не теряет cover-кроп.
- Fullscreen open/close не ломается.
- Нет hydration/runtime/browser console errors.
